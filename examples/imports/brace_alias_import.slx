import colors;
import styles using {Background as Bg};

stylesheet MyStyle(color: int) uses Background(color) {
	styles {
		backgroundColor: Color(inner:color)
	}
}

component MyComponent {
	Div {
		style: MyStyle(0xff0000),
		Text {
			text: "Brace alias import"
		}
	}
}
