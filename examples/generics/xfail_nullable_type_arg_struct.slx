// xfail: `int?` as a generic type argument of a struct fails to parse
// ("Unexpected token: 'Question'"). Components accept it (component_nullable.slx).
object Option<T> {
    value: T
}

func main(): int {
    let some = Option<int?>(value: 42);
    some.value
}