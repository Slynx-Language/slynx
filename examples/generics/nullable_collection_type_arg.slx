// xpass: `([]int)?` as a generic type argument fails to parse.
import option using {Option};
func identity<T>(x: T): T {
    x
}

func main(): Option<[]int> {
    let v = identity<Option<[]int>>({1,2,3});
    v

}
