// A generic function over a fixed-size array parameter works: `len<int>` is
// specialized with `[4]int` and the array literal unifies with it.
func len<T>(items: [4]T): int {
    4
}

func main(): int {
    len<int>([1,2,3,4])
}