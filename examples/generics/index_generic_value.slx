// Indexing a generic value whose type argument is a vector collection works:
// the HIR defers the index-check to monomorphization, which substitutes the
// concrete `[]int` and retypes the index expression.
func identity<T>(x: T): T {
    x
}

func main(): int {
    identity<[]int>({1,2,3})[0]
}
