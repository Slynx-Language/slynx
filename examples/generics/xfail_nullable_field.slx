// xfail: `T?` on a generic struct field fails with
// "IR internal error: type 'T' is not recognized by the IR".
object Box<T> {
    data: T?
}

func main(): int {
    let b = Box<int>(data: 42);
    b.data
}