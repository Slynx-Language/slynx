// A nullable generic return type works: `identity<int>` is specialized to
// `func(...) -> int?`.
import option using {Option};
func identity<T>(x: T): Option<T> {
    Option.Some(x)
}

func main(): Option<int> {
    identity<int>(42)
}
