// xfail: indexing a generic struct field of vector type fails with
// "Expression cannot be indexed. Type is 'T'".
object Box<T> {
    data: T
}

func main(): int {
    let b = Box<[]int>(data: {1,2,3});
    b.data[0]
}